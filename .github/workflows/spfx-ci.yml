name: Build and Deploy SPFx to SharePoint App Catalog

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: windows-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js (SPFx compatible)
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Install dependencies
      - name: Install dependencies
        run: |
          npm install
          npm install -g gulp

      # 4️⃣ Build SPFx solution
      - name: Build solution
        run: |
          gulp clean
          gulp build --ship
          gulp bundle --ship
          gulp package-solution --ship

      # 5️⃣ Install latest PnP PowerShell
      - name: Install PnP PowerShell
        shell: pwsh
        run: |
          Install-Module PnP.PowerShell -Scope CurrentUser -Force -SkipPublisherCheck

      # 6️⃣ Validate required secrets
      - name: Validate Secrets
        shell: pwsh
        run: |
          if ([string]::IsNullOrWhiteSpace("${{ secrets.CLIENT_ID }}")) {
            throw "CLIENT_ID secret is missing!"
          }

          if ([string]::IsNullOrWhiteSpace("${{ secrets.CERT_PASSWORD }}")) {
            throw "CERT_PASSWORD secret is missing!"
          }

          if ([string]::IsNullOrWhiteSpace("${{ secrets.CERT_BASE64 }}")) {
            throw "CERT_BASE64 secret is missing!"
          }

          Write-Host "✅ Secrets validated."

      # 7️⃣ Create certificate file from Base64
      - name: Create certificate file
        shell: pwsh
        run: |
          $bytes = [Convert]::FromBase64String("${{ secrets.CERT_BASE64 }}")
          [IO.File]::WriteAllBytes("cert.pfx", $bytes)
          Write-Host "✅ Certificate file created."

      # 8️⃣ Debug solution folder (helps avoid null reference error)
      - name: Debug solution folder
        shell: pwsh
        run: |
          Write-Host "Listing sharepoint/solution folder:"
          Get-ChildItem -Recurse sharepoint/solution

      # 9️⃣ Deploy to SharePoint
      - name: Deploy to SharePoint
        shell: pwsh
        run: |
          Import-Module PnP.PowerShell -Force

          $securePassword = ConvertTo-SecureString "${{ secrets.CERT_PASSWORD }}" -AsPlainText -Force

          Connect-PnPOnline `
            -Url "https://wb0wj-admin.sharepoint.com" `
            -ClientId "${{ secrets.CLIENT_ID }}" `
            -Tenant "wb0wj.onmicrosoft.com" `
            -CertificatePath "cert.pfx" `
            -CertificatePassword $securePassword

          Write-Host "✅ Connected to SharePoint."

          # Auto-detect sppkg file
          $package = Get-ChildItem "sharepoint/solution/*.sppkg" | Select-Object -First 1

          if (-not $package) {
            throw "No .sppkg file found in sharepoint/solution folder!"
          }

          Write-Host "Deploying package: $($package.FullName)"

          Add-PnPApp `
            -Path $package.FullName `
            -Scope Tenant `
            -Publish `
            -Overwrite

          Write-Host "🚀 Deployment completed successfully!"
